name: Build iOS IPA

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.2.0'

    - name: Install Tools
      run: brew install xcodegen imagemagick

    - name: Generate App Icon Assets
      run: |
        mkdir -p Media.xcassets/AppIcon.appiconset
        
        if [ -f "icon.svg" ]; then
          magick -background none icon.svg -resize 1024x1024 Media.xcassets/AppIcon.appiconset/icon_1024.png
        else
          magick -size 1024x1024 xc:blue Media.xcassets/AppIcon.appiconset/icon_1024.png
        fi
        
        cat << 'EOF' > Media.xcassets/AppIcon.appiconset/Contents.json
        {
          "images": [
            {
              "filename": "icon_1024.png",
              "idiom": "universal",
              "platform": "ios",
              "size": "1024x1024"
            }
          ],
          "info": {
            "author": "xcode",
            "version": 1
          }
        }
        EOF

    - name: Generate Xcode Project
      run: xcodegen generate

    - name: Build Archive
      run: |
        xcodebuild archive \
          -project MyBrowser.xcodeproj \
          -scheme MyBrowser \
          -sdk iphoneos \
          -archivePath build/MyBrowser.xcarchive \
          CODE_SIGNING_ALLOWED=NO

    - name: Export Unsigned IPA
      run: |
        mkdir -p build/Payload
        cp -R build/MyBrowser.xcarchive/Products/Applications/*.app build/Payload/
        cd build
        zip -r MyBrowser.ipa Payload

    - name: Upload IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MyBrowser-Unsigned-IPA
        path: build/MyBrowser.ipa